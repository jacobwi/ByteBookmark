import { render } from '@testing-library/react';

import { Popup } from '../../../src/pages/popup/Popup';

describe('Popup Component', () => {
  it('should render without crashing', () => {
    render(<Popup />);
    // Expectations or assertions here...
  });
});
